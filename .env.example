# Airflow UID/GID (use `id -u` to get your UID)
AIRFLOW_UID=50000
AIRFLOW_GID=0

# Redis password - GENERATE YOUR OWN!
# Example: openssl rand -base64 32
REDIS_PASSWORD=your_secure_redis_password_here

# Airflow Fernet key for encryption - GENERATE YOUR OWN!
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW__CORE__FERNET_KEY=your_fernet_key_here

# Airflow webserver credentials
_AIRFLOW_WWW_USER_USERNAME=airflow
_AIRFLOW_WWW_USER_PASSWORD=your_secure_password_here

# Additional Python packages (optional)
_PIP_ADDITIONAL_REQUIREMENTS=

# Project directory
AIRFLOW_PROJ_DIR=.
