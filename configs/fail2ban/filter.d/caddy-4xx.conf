# Бёрсты 401/403 в JSON access-логе Caddy (Caddyfile: format json, rfc3339).
# client_ip — реальный IP клиента: userland-proxy=false, DNAT сохраняет src.
[Definition]
failregex = ^.*"client_ip":"<HOST>".*"status":(?:401|403)[,}]
ignoreregex =
