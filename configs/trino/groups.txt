# Формат: группа:юзер1,юзер2  (hot-reload 30с, рестарт Trino не нужен)
# Обычным юзерам (аналитики/viewers) запись здесь НЕ нужна: catch-all в
# rules.json даёт любому не-машинному юзеру SELECT на silver/gold автоматом
# (включая Superset-impersonation и локального админа Superset).
# Здесь остаются только админы платформы (полный доступ в Trino).
# Trino берёт группы ТОЛЬКО отсюда (oauth2 groups-field в 482 нет).
platform-admins:sergey
