# Формат: группа:юзер1,юзер2  (hot-reload 30с, рестарт Trino не нужен)
# Нового аналитика добавляй сюда ТОЖЕ (плюс Keycloak) — иначе Superset
# SQL Lab (impersonation) не даст ему прав группы analysts.
# admin — локальный админ Superset: его дашборды под impersonation тоже
# должны читать данные (read-only достаточно).
analysts:test-analyst,admin
# ВАЖНО: Trino берёт группы ТОЛЬКО отсюда (oauth2 groups-field в 482 нет),
# поэтому админы платформы тоже перечисляются здесь.
platform-admins:sergey
