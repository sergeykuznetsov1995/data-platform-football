# File-based access control (docs/design/analyst-access.md, фаза 3).
# Машинные аккаунты — полный доступ, аналитики — SELECT только silver/gold.
# rules.json перечитывается каждые 30с без рестарта.
access-control.name=file
security.config-file=/etc/trino/rules.json
security.refresh-period=30s
